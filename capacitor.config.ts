import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moustachio.app',
  appName: 'moustachio',
  webDir: 'dist/moustachio/browser'
};

export default config;
